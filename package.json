{
  "name": "ailit-framework-kr",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "AI 시대의 학습자 역량강화 — AILit 프레임워크 한국어판",
  "scripts": {
    "dev": "npm run assets && vite",
    "assets": "node scripts/build-asset-manifest.mjs",
    "prebuild": "npm run assets",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
