# Claude Code 작업 지시서 — AILit 프레임워크 한국어판 사이트

아래 내용을 **Claude Code에 그대로 붙여넣으세요.** 같은 폴더에 `content.json`과 `ailit-framework-kr.html`을 두고 시작합니다.

---

## 프롬프트 (여기부터 복사)

이 폴더에 있는 두 파일을 읽고, 정적 웹사이트를 만들어 줘.

**소스 파일**
- `content.json` — 지식 진술문 17개, 기능 7개, 태도 6개, 4개 영역 19개 역량(각 기초/중급/심화 3단계 + 교실 사례), 용어집 40개 항목. 이미 한국어로 번역되어 있음. **번역문을 임의로 고치지 마.**
- `ailit-framework-kr.html` — 위 데이터에 더해 1~3장 서술부, 각 박스(BOX), 부록 1·2, 라이선스 고지가 HTML 본문에 들어 있음. 이 산문 부분을 그대로 가져와 쓸 것.
- `assets/` — 원문에서 추출한 삽화·도판 26장(PNG, 투명 배경). 배치 위치와 캡션은 `assets/CREDITS.md`에 표로 정리되어 있음.

**만들 것**
Vite + 순수 TypeScript(프레임워크 없이) 정적 사이트. 빌드 결과가 `dist/`에 떨어지고 GitHub Pages에 그대로 올라갈 수 있어야 함.

### 레이아웃

3단 구성. 데스크톱 기준:

```
┌──────────────────────────────────────────────────────────┐
│  상단 바 (고정, 56px)                                      │
├──────────┬────────────────────────────────┬──────────────┤
│          │                                │              │
│  좌측    │   본문 (16:9 프레임)            │  우측 목차    │
│  영역     │                                │  (스크롤스파이)│
│  칩       │   aspect-ratio 16/9 를 최대     │              │
│  (200px) │   폭 기준으로 잡은 와이드 컨테이너 │   240px      │
│          │                                │              │
└──────────┴────────────────────────────────┴──────────────┘
```

- **본문 컨테이너**: `max-width: 1440px`, 내부 읽기 영역은 16:9 비례를 기준으로 잡되(1440 × 810 그리드 감각), 텍스트 줄길이가 90자를 넘지 않게 2단 또는 좌우 여백으로 조절해. 긴 산문은 절대 화면 폭을 꽉 채우지 말 것.
- **우측 목차**: `position: sticky; top: 56px`. 접이식 2단(아래 "우측 목차는 접이식 2단으로" 참조). IntersectionObserver로 현재 위치를 하이라이트하고, 클릭하면 해당 절로 스무스 스크롤. 1024px 미만에서는 상단 드로어 버튼으로 전환.
- **좌측**: 네 영역(마주하기/창작하기/관리하기/만들어가기) 색상 칩. 1280px 미만에서 숨김.

### 문서 구조

본문은 아래 순서로 흐름. 괄호 안은 목차에 쓸 짧은 라벨.

```
01 서론 (서론)                        — AI 리터러시란 / AI란 / 청소년의 AI 사용
02 프레임워크의 기초 (기초)             — 네 영역 / 역량의 구조 / 이해관계자 / 교사의 역할
03 개발 과정 (개발)                    — 초안 개정 / 윤리 / 인접 학문 / 선행 프레임워크
04 지식·기능·태도 (지식·기능·태도)      — 지식 1.1–4.3 / 기능 7 / 태도 6
04 역량 (역량)                         — AI와 마주하기 E1–7 / AI와 창작하기 C1–4
                                        AI 관리하기 M1–4 / AI 만들어가기 S1–4
05 용어집 (용어집)
06 부록 (부록)
```

### 우측 목차는 접이식 2단으로

전체를 펼쳐 두면 20줄이 넘어 스크롤이 생기니, **1단만 항상 보이고 현재 읽는 절의 2단만 펼쳐지는** 아코디언으로 만들어. 다른 절로 스크롤하면 이전 절은 자동으로 접힘.

기본 상태 (10줄):

```
  서론
  기초
  개발
▾ 지식·기능·태도
    지식
    기능
    태도
  역량
  용어집
  부록
```

역량 절을 읽는 중일 때:

```
  서론
  기초
  개발
  지식·기능·태도
▾ 역량
    E  AI와 마주하기
    C  AI와 창작하기
    M  AI 관리하기
    S  AI 만들어가기
  용어집
  부록
```

세부 규칙

- **영역 이름은 반드시 정식 명칭 전체를 쓸 것.** "마주하기"가 아니라 "AI와 마주하기". 줄이지 마.
- **지식·기능·태도는 예외적으로 항상 펼쳐 둘 것.** 이 세 축이 프레임워크의 뼈대라 접히면 안 됨. 나머지 절의 아코디언 규칙과 무관하게 상시 노출.
- 2단 항목은 위 목록이 전부. 역량 19개를 낱개로 나열하지 말 것. 대신 영역 라벨을 누르면 그 아래 `E1 E2 E3 …` 코드 칩이 한 줄로 깔리는 3단을 두고, 칩 클릭으로 개별 역량에 도달하게 해. 칩은 24px 정사각 모노스페이스.
- 지식 항목도 같은 방식으로, 누르면 `1.1 1.2 … 4.3` 칩이 깔리게 해.
- 1단 항목 왼쪽에 4px 세로 바를 두고, 역량 절에서는 현재 영역 색(`#12428E` / `#6E9410` / `#D8431F` / `#5C74B8`)으로 칠해. 목차가 지금 어느 영역에 있는지를 색으로 알려 주는 역할.
- 사용자가 직접 접거나 펼친 뒤에는 스크롤에 의한 자동 전환을 잠시 멈출 것. 읽는 중에 목차가 제멋대로 움직이면 성가심.
- 목차 하단에 진행률 막대 하나. 문서가 길어서 위치 감각이 필요함.
- 폭 240px 고정, `overflow-y: auto`, 접힌 상태에서는 스크롤바가 뜨지 않아야 함.

### 디자인 토큰

`ailit-framework-kr.html`의 `:root` CSS 변수를 그대로 승계할 것.

- 잉크 `#0B1B33` / 종이 `#F3F5FA` / 카드 `#FFFFFF` / 괘선 `#D6DDE9`
- 영역색: 마주하기 `#12428E` · 창작하기 `#6E9410` · 관리하기 `#D8431F` · 만들어가기 `#5C74B8`
- 서체: 표제 `Noto Serif KR` 700/900, 본문 `Pretendard Variable`, 코드·라벨 `IBM Plex Mono`
- 역량 코드 칩(E1, M3 등)과 지식 코드(2.5 등)는 모노스페이스. 이 번호 체계는 원문의 실제 참조 체계이므로 장식이 아니라 정보로 다룰 것.

### 삽화 배치

`assets/CREDITS.md`의 표를 그대로 따를 것. 핵심만 옮기면:

- **도판 5장** (`fig1`~`fig5`) — 해당 절 안에 `<figure>`로 배치하고 `<figcaption>`에 CREDITS.md의 한국어 캡션을 넣어. 도판 안의 라벨은 영문이므로, 그림 1·4·5 아래에는 영문-한국어 대응표를 작은 표로 함께 둘 것 (예: Engage with AI → AI와 마주하기).
- **영역 오프너 4장** — 각 영역 섹션 헤더 옆이나 아래에 배치. 배경이 투명하므로 영역 색상 위에 올려도 됨.
- **영역 배지 4장** — 역량 목록 상단의 작은 마크.
- **이해관계자 아이콘 5장** — 이해관계자 카드 각각의 상단에.
- **본문 삽화 4장** — `illus-teachers.png`는 세로로 긴 비율(1096×1901)이니 2단 배치 시 오른쪽 열에 두는 게 자연스러움.

이미지 처리: `loading="lazy"`, 의미 있는 한국어 `alt`, 그리고 시각 정보가 아니라 장식으로 쓰는 경우(배지 등)에는 `alt=""` + `aria-hidden="true"`. 도판은 클릭하면 확대되는 라이트박스를 붙여도 좋음.

**삽화 크레딧**을 푸터에 반드시 명기: 삽화 Abiyasa Adiguna, © OECD / European Union, 2026, CC BY 4.0. OECD·유럽집행위원회 로고와 표지 이미지는 라이선스 적용 대상이 아니어서 애초에 포함하지 않았으니, 어떤 형태로도 추가하지 말 것.

### 기능 요구사항

1. **역량 카드** — 기초/중급/심화를 데스크톱에서는 3열 병렬, 태블릿 이하에서는 탭 전환. 각 단계에 "교실에서" 사례를 함께 표시.
2. **역량 태그** — 각 역량이 어떤 지식·기능·태도에 뿌리내리는지 칩으로 표시하고, 지식 칩을 누르면 해당 지식 진술문으로 이동.
3. **전역 검색** — 상단 바에 검색창. 역량·지식·용어를 제목과 본문에서 필터링. Cmd/Ctrl+K로 열기.
4. **용어집** — 검색 필터. 본문에서 용어가 처음 나올 때 점선 밑줄 + 호버 툴팁으로 정의 노출(선택 구현).
5. **접근성** — 키보드 포커스 링, `prefers-reduced-motion` 존중, 목차는 `<nav aria-label="목차">`, 모바일 375px까지 무너지지 않을 것.
6. **인쇄 스타일** — `@media print`에서 목차·상단바 숨기고 역량 3단계를 세로로 펼칠 것.

### 하지 말 것

- localStorage/sessionStorage 사용 금지
- 원문 도판·삽화·OECD/EU 로고 사용 금지 (라이선스 제한 대상)
- 번역문 임의 수정, 요약, 축약 금지
- 푸터의 CC BY 4.0 고지와 "원문과 번역문 사이에 불일치가 있을 경우 원문만 유효" 문구는 반드시 유지

### 배포 설정도 같이 만들어 줘

배포처는 **Railway**야. GitHub Pages가 아니니 하위 경로 처리는 필요 없어.

- `vite.config.ts`에 `base: '/'`
- `Dockerfile` — 멀티스테이지. 1단계 `node:22-alpine`에서 `npm ci && npm run build`, 2단계 `nginx:alpine`에 `dist/`만 복사
- `nginx.conf.template` — Railway가 주입하는 `$PORT`를 `listen`에 쓰도록 `envsubst`로 치환. 정적 파일은 1년 캐시, `index.html`은 `no-cache`, gzip 활성화
- `.dockerignore` — `node_modules`, `dist`, `.git`
- `railway.json` — 빌더를 `DOCKERFILE`로 지정
- `README.md` — 로컬 실행법, 배포법, 출처와 라이선스

자세한 요구사항은 같은 폴더의 `DEPLOY.md`에 있으니 그대로 따라 줘.

먼저 파일 구조와 컴포넌트 계획을 보여 주고, 내가 확인하면 그때 구현 시작해.

## 프롬프트 (여기까지)

---

## 배포 절차

### 1. 준비

```bash
mkdir ailit-kr && cd ailit-kr
# ailit-assets.zip 을 이 폴더에 풀면 아래 구조가 됩니다
#   content.json
#   ailit-framework-kr.html
#   assets/  (PNG 26장 + CREDITS.md)
unzip ailit-assets.zip
git init
claude
```

Claude Code가 열리면 위 프롬프트를 붙여넣습니다.

### 2. 로컬 확인

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # dist/ 생성
npm run preview  # 빌드 결과 확인
```

### 3. Railway 배포

같은 폴더의 **`DEPLOY.md`** 를 보세요. CLI 방식과 GitHub 연동 방식, 도메인 연결까지 순서대로 정리해 두었습니다.

---

## 원문 출처

OECD / European Union (2026). *Empowering learners for the age of AI: An AI literacy framework for primary and secondary education.* OECD Publishing, Paris. https://doi.org/10.1787/65cd27d4-en
CC BY 4.0 · © OECD / European Union, 2026

원문 사이트 https://ailiteracyframework.org/
